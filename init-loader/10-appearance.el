;; Hide splash message
(setq inhibit-startup-message t)

;; Hide scroll bar
(scroll-bar-mode 0)

;; Hide menu bar
(menu-bar-mode 0)

;; Show column number
(column-number-mode 1)

;; Show line numbers
;; conflict with gitgutter.el
;; (global-linum-mode 1)

;; Hide tool bar
(tool-bar-mode 0)
